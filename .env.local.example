## Frontend (Vite) environment variables
# GraphQL endpoint for production (Render backend URL)
# Example: https://loanshark-backend.onrender.com/graphql
VITE_GRAPHQL_URL=

# Admin API key to attach to GraphQL requests when not using borrower auth
# Must match backend API_KEY on Render for admin requests
VITE_API_KEY=

# Google Gemini API key for AI insights in LoanDetail
# Do NOT reuse backend API_KEY; use a separate Google API key
GEMINI_API_KEY=